# sync.sh

`rnfmac brew sync` — installs/cleans up Homebrew packages against the host Brewfile.

## Overview

Script to sync installed Homebrew packages against the host Brewfile
Version: 1.0
Author: Rohit Narayanan

## Index

* [sync_packages](#sync_packages)

### sync_packages

Run `brew bundle install` + `cleanup` against `BREWFILE`.

_Function has no arguments._

