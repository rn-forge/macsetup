# AGENTS.md

This repository keeps a single set of agent instructions, in [CLAUDE.md](CLAUDE.md).

**Read `CLAUDE.md` now and follow it as if its contents appeared here.** It maps the repo,
states the conventions and invariants you must respect when changing code, and points at
[README.md](README.md) for user-facing detail (commands, install, machine config, tasks).

Do not add guidance to this file — it would drift out of sync. Put it in `CLAUDE.md`.
